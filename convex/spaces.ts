import { v } from "convex/values";
import { mutation, query } from "./_generated/server";
import { getAuthUserId } from "@convex-dev/auth/server";
export const getMySpaces = query({ args: {}, handler: async (ctx) => { const userId = await getAuthUserId(ctx); if (!userId) return []; return await ctx.db.query("spaces").withIndex("by_user", (q) => q.eq("userId", userId)).collect(); } });
export const getSpaceBySlug = query({ args: { slug: v.string() }, handler: async (ctx, args) => { return await ctx.db.query("spaces").withIndex("by_slug", (q) => q.eq("slug", args.slug)).first(); } });
export const getSpaceById = query({ args: { spaceId: v.id("spaces") }, handler: async (ctx, args) => { const userId = await getAuthUserId(ctx); if (!userId) return null; const space = await ctx.db.get(args.spaceId); if (!space || space.userId !== userId) return null; return space; } });
export const createSpace = mutation({ args: { name: v.string(), slug: v.string(), description: v.optional(v.string()), theme: v.union(v.literal("light"),v.literal("dark")), primaryColor: v.string(), thankYouMessage: v.string(), questionPrompt: v.string(), collectCompany: v.boolean(), collectRole: v.boolean() }, handler: async (ctx, args) => { const userId = await getAuthUserId(ctx); if (!userId) throw new Error("Not authenticated"); const existing = await ctx.db.query("spaces").withIndex("by_slug", (q) => q.eq("slug", args.slug)).first(); if (existing) throw new Error("Slug already taken."); return await ctx.db.insert("spaces", { ...args, userId }); } });
export const updateSpace = mutation({ args: { spaceId: v.id("spaces"), name: v.optional(v.string()), description: v.optional(v.string()), theme: v.optional(v.union(v.literal("light"),v.literal("dark"))), primaryColor: v.optional(v.string()), thankYouMessage: v.optional(v.string()), questionPrompt: v.optional(v.string()) }, handler: async (ctx, args) => { const userId = await getAuthUserId(ctx); if (!userId) throw new Error("Not authenticated"); const { spaceId, ...updates } = args; const space = await ctx.db.get(spaceId); if (!space || space.userId !== userId) throw new Error("Not found"); await ctx.db.patch(spaceId, Object.fromEntries(Object.entries(updates).filter(([,v]) => v !== undefined))); } });
export const deleteSpace = mutation({ args: { spaceId: v.id("spaces") }, handler: async (ctx, args) => { const userId = await getAuthUserId(ctx); if (!userId) throw new Error("Not authenticated"); const space = await ctx.db.get(args.spaceId); if (!space || space.userId !== userId) throw new Error("Not found"); const ts = await ctx.db.query("testimonials").withIndex("by_space", (q) => q.eq("spaceId", args.spaceId)).collect(); for (const t of ts) await ctx.db.delete(t._id); await ctx.db.delete(args.spaceId); } });
export const getSpaceStats = query({ args: { spaceId: v.id("spaces") }, handler: async (ctx, args) => { const userId = await getAuthUserId(ctx); if (!userId) return null; const space = await ctx.db.get(args.spaceId); if (!space || space.userId !== userId) return null; const all = await ctx.db.query("testimonials").withIndex("by_space", (q) => q.eq("spaceId", args.spaceId)).collect(); const approved = all.filter(t => t.status === "approved"); return { total: all.length, approved: approved.length, pending: all.filter(t => t.status === "pending").length, avgRating: approved.length > 0 ? Math.round(approved.reduce((s,t) => s+t.rating,0)/approved.length*10)/10 : 0 }; } });
